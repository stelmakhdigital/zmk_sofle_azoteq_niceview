## For local build firmware

### Initializing and installing environment packages

> Command in VSCode Terminal doesn't working - use terminal

```bash
make start
```

### Build

```bash
make all
```

> The finished firmware will be stored in the directory `firmware`


## For build on Github Actions

> It happens automatically when changes are committed